# docs/

Supplementary documentation — architecture deep-dives, ADRs, runbooks — that isn't part of the
always-loaded `CLAUDE.md`/`AGENTS.md` context. Claude Code reads files here on demand when a task
needs that depth, rather than automatically on every session. (Not to be confused with
`src/docs/`, which holds Storybook's own MDX pages.)
