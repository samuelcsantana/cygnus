# TODO: domínio de produção ainda não registrado — preencher antes do primeiro deploy.
VITE_API_BASE_URL=
# TODO: sem projeto Sentry criado ainda — sem DSN, o app roda normalmente (error-reporting.ts é um no-op).
VITE_SENTRY_DSN=
